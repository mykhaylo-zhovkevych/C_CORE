#include <stdio.h>
// This line is a preprocessor directive that tells the C compiler to include the contents of the Standard Input Output header file (stdio.h) before actual compilation begins.


int main(void) {
// This line calls the printf function to output the string Hello, World followed by a newline character (\n) to the standard output, usually the console or terminal.
    // Hello, Wolrd is the argument 
    printf("Hello, World \n");
    printf("line 2!\n");

// This statement ends the main function and returns a value of 0 to the operating system, signaling that the program executed successfully.
    return 0;

}